# ChartDB Frontend Environment Variables (Production)
# For unified deployment - backend serves frontend

# Backend API URL (same origin, relative path)
VITE_API_URL=/api

# WebSocket URL for real-time collaboration (same origin)
VITE_WS_URL=/ws
